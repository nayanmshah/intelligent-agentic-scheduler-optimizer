---
title: Intelligent Agentic Scheduling Optimizer — System Context
---
flowchart TB
    subgraph people ["People"]
        OP["Front-Desk Operator<br/>Operator Console"]
        OM["Office Manager / Ops Lead<br/>Policy · Traces · Scorecard"]
    end

    subgraph app ["Scheduling Optimizer — single process, one machine"]
        UI["React + TypeScript SPA"]
        API["FastAPI application<br/>orchestrator · reasoner · eval"]
        MEM["In-memory session state<br/>schedule copy · holds · traces"]
    end

    subgraph files ["Committed files — the system of record"]
        SEED["Seed dataset JSON<br/>+ SEED_DIGEST"]
        FIX["LLM response fixtures<br/><i>the degraded path</i>"]
        GOLD["Golden set — 40 labelled requests"]
    end

    subgraph external ["External"]
        LLM["Anthropic Claude API<br/>extract · verify · explain"]
        OPIK["Opik — self-hosted, localhost<br/><i>optional, fire and forget</i>"]
    end

    OP --> UI
    OM --> UI
    UI -->|"HTTP / JSON"| API
    API --> MEM
    API -->|"read at boot"| SEED
    API -->|"fallback when the model is unreachable"| FIX
    API -->|"read by harness"| GOLD
    API -->|"3 calls per request — the shipped default"| LLM
    API -.->|"fire and forget"| OPIK
